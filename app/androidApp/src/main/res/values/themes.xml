<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Post-splash app theme (Compose owns the actual UI; this is just the window baseline). -->
    <style name="Theme.PosTerminal" parent="@android:style/Theme.Material.Light.NoActionBar" />

    <!-- Launch theme: standard core-splashscreen with the app launcher icon. -->
    <style name="Theme.PosTerminal.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.PosTerminal</item>
    </style>
</resources>
